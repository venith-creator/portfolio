{
  "build": {
    "useNixpacks": true,
    "builder": "nixpacks",
    "buildCommand": "python manage.py migrate"
  },
  "deploy": {
    "startCommand": "gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
  }
}